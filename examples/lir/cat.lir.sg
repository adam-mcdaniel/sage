const putchar = proc(ch: Char) -> None = core {
    put-char [SP]
    pop
}, getchar = proc() -> Char = core {
    next SP
    get-char [SP]
} in let ch = getchar() in while ch {
    putchar(ch);
    ch = getchar();   
}